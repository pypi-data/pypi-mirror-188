[tool.poetry]
name = "pypi-poetry-project"
version = "0.1.0"
description = ""
authors = ["antoniotoineto <netocaastro10@gmail.com>"]
readme = "README.md"
packages = [{include = "pypi_poetry_project"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
