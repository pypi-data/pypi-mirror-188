[tool.poetry]
name = "alpha-kentaurus-macos"
version = "0.1.3"
description = "Not what you're looking for"
authors = ["nope"]
readme = "README.md"
packages = [{include = "src/alpha_kentaurus"}]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
