{
    "sessions": {
        "platform": {
            "my-custom-session": {
                "app-key": "${DESKTOP_APP_KEY}",
                "username": "${EDP_USERNAME}",
                "password": "${EDP_PASSWORD}"
            }
        }
    }
}