[tool.poetry]
name = "roll-the-dice"
version = "0.1.0"
description = ""
authors = ["Guilherme <marquesguilherme3@gmail.com>"]
readme = "README.md"
packages = [{include = "roll_the_dice"}]

[tool.poetry.dependencies]
python = "^3.8"
typer = "^0.7.0"

[tool.poetry.scripts]
dice = "roll_the_dice.cli:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
