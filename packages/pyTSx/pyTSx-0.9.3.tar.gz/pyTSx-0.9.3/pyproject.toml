# pyproject.toml

[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyTSx"
version = "0.9.3"
description = "Time Series Analysis with Python and Spark."
#longdescription = "Simplify time series analysis with Python and PySpark."
readme = "README.md"
authors = [{ name = "Dr. Mirko Kämpf", email = "mirko.kaempf@gmail.com" }]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = ["time series", "DFA", "MF-DFA", "complex systems"]
dependencies = [
    "html2text",
    'tomli; python_version < "3.11"',
]
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["black", "bumpver", "isort", "pip-tools", "pytest"]

[project.urls]
Homepage = "https://github.com/kamir/pyTSx"

[project.scripts]
pyTSxdemo = "pyTSx.demo.__main__:main"