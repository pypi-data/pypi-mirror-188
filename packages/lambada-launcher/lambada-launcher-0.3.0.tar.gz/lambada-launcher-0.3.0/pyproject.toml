[project]
name = "lambada-launcher"
description = "Basic job launcher"
version = "0.3.0"
authors = [
  {name = "Fabien André", email = "fabien@andre.bzh" },
]
license = {text ="WTFPL"}
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python",
]
readme = "README.md"

dependencies = [
  "toml",
  #"tkinter", #not installed through pip
]

[project.urls]
repository = "https://gitlab.com/fabienandre/lambada"

[project.scripts]
lambada = "lambada.__main__:main"
