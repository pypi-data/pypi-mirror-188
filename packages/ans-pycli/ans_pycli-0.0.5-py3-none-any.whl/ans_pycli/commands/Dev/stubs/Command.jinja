from argparse import ArgumentParser

from ..BaseCommand import Command


class {{ name }}Command(Command):
    _name = "{{ name|lower }}"
    _description = "{{ description }}"

    @classmethod
    def _func(cls):
      print("{} is Done!".format(cls.name))

    @classmethod
    def init_parser(self, parser: ArgumentParser):
        pass
