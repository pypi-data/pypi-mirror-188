[tool.poetry]
name = "colipy-cli"
version = "0.1.1.dev1"
license = "GPL-3.0-or-later"
authors = ["Johannes Lübke <60359335+johannes-luebke@users.noreply.github.com>"]
description = "Colipy is a command line ui library for python."
readme = "README.md"
repository = "https://github.com/johannes-luebke/colipy"
packages = [{ include = "colipy" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
black = "^22.12.0"
isort = "^5.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
