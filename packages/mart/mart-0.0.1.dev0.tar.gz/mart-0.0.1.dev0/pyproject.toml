[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mart"
version = "0.0.1-dev"
dependencies = [
    'numpy',
    'scipy',
    'matplotlib'
]

[tool.setuptools]
include-package-data = false